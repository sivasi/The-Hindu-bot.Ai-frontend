# Local backend
VITE_API_URL=http://localhost:3001

# Production (Netlify): leave empty or set to the site origin so requests stay same-origin.
# VITE_API_URL=
# VITE_API_URL=https://the-hindu-bot.netlify.app
# Never point the browser at http://34.93.159.106:30001 from the HTTPS site.